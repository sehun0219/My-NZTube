extends base
include mixins/video

block content 
  
  form(method="GET").search__form 
    input(placeholder="Search by Title", type="text", name="keyword")
    button
      i.fa.fa-search
  div.video-grid 
    each video in videos 
      +video(video)
    else 
      span.empty_message No videos yet  


